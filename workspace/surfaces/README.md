# Surface library

This is the durable access point for TIFXYZ surfaces. `public/` contains
downloaded public surfaces and `campaign-x/` contains surfaces grown by this
campaign. Both point to the preserved legacy library during migration so that
no multi-gigabyte data is duplicated.
